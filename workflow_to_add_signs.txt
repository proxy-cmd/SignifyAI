1. Record clips for intent A (option 4).
2. Record clips for intent B (option 4 again, with a different intent).
3. Use a signer id when recording. Keep the same signer id for one person.
4. Try at least 8 to 12 clips per intent per signer.
5. Build a dataset version (option 5).
6. Check dataset health (auto-shown after build and before train).
7. Train model (option 6).
8. Option 6 benchmarks multiple algorithms and keeps one best global model.
9. Evaluate (option 7).
10. Promote (option 8).

Notes:
- If signer count is low, dataset builder uses label-based fallback split.
- Intent text is auto-normalized to intent_id format (snake_case).
- For usable training, keep at least 2 intent labels in train split.
