# SignifyAI Rebuild (Batch 1)

Minimal, fast, hardcoded hand-sign recognizer.

Run:

```bash
python -u .\run_this.py
```

Controls:
- `q` or `Esc`: quit
- `v`: toggle voice
- `r`: reset smoothing/speech memory
